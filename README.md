# app- please fix the syntax error
import kivy
from kivy.app import App
from kivy.uix.label import Label


class SparatanApp(App)
    def build_config(self):

        return Label(text="This is spartaaa!!!")


if __name__ == "__main__"
    SparatanApp().run()
